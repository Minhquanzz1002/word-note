spring:
  application:
    name: toeic
  profiles:
    active: local

mybatis:
  mapper-locations: classpath:/mapper/**/*.xml
  type-aliases-package: com.vn.toeic.entity
  configuration:
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl